server {
    listen 80;
    server_name app.framestudios.it;

    # Redirect HTTP → HTTPS
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name app.framestudios.it;

    # SSL — gestito da certbot (Let's Encrypt)
    ssl_certificate     /etc/letsencrypt/live/app.framestudios.it/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/app.framestudios.it/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    # Aumenta limite upload (foto/video)
    client_max_body_size 200M;

    location / {
        proxy_pass         http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
